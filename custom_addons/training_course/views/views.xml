<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="training.course.list">
      <field name="name">training.course list</field>
      <field name="model">training.course.training.course</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="training.course.action_window">
      <field name="name">training.course window</field>
      <field name="res_model">training.course.training.course</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="training.course.action_server">
      <field name="name">training.course server</field>
      <field name="model_id" ref="model_training.course_training.course"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="training.course" id="training.course.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="training.course.menu_1" parent="training.course.menu_root"/>
    <menuitem name="Menu 2" id="training.course.menu_2" parent="training.course.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="training.course.menu_1_list" parent="training.course.menu_1"
              action="training.course.action_window"/>
    <menuitem name="Server to list" id="training.course" parent="training.course.menu_2"
              action="training.course.action_server"/>
-->
  </data>
</odoo>
